fastapi
uvicorn[standard]
python-dotenv
pydantic>=2
APScheduler>=3.10
httpx
gspread
google-auth
tzdata; platform_system == 'Windows'
uv
# Use LINE SDK v2 series (v3 has breaking changes in import paths)
line-bot-sdk>=3.17.0,<4
