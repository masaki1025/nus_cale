########################################
# .env example for Nus Cale
# 各値は必要に応じて設定してください
########################################

# 基本
ENV=development
PORT=8000
TZ=Asia/Tokyo

# シフトデータ取得
# パターンA: ローカルCSVを使用する場合（例を有効化して使ってください）
# SHEETS_ID=sheet/sample_schedule_2025_09.csv
# パターンB: GoogleスプレッドシートIDを指定する場合
SHEETS_ID=
# 任意: 読み込むワークシート（タブ）を指定（片方のみでOK）
SHEETS_WORKSHEET_TITLE=
SHEETS_GID=

# Google（Sheets 利用時のみ必須）
GOOGLE_PROJECT_ID=
# サービスアカウントのJSON（ファイルパス or JSON文字列）
GOOGLE_SERVICE_ACCOUNT_JSON=

# LINE Messaging API（Push送信/Webhookで使用）
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=

# スケジューラ（例: 前日19時と当日7時に通知を実行）
NOTIFY_SCHEDULES=19:00,07:00

# ユーザー設定（JSONファイルパス）
# 例のサンプル: app/data/users.sample.json をコピーして編集
USERS_JSON=
# 既定のユーザーID（未指定実行時に使用。空なら最初のユーザー）
DEFAULT_USER_ID=

########################################
# 複数ユーザー運用（シートを分ける場合の考え方）
########################################
# 推奨: USERS_JSON にユーザーごとのシート設定を持たせます。
# 例）users.json の各ユーザー要素に以下を追加し、個別に参照先を分けます。
#   {
#     "id": "alice",
#     "my_name": "佐藤美咲",
#     "line_user_id": "Uxxxxxxxx_alice",
#     "sheets_id": "1AbCdEf...",                 # 個別のスプレッドシートID
#     "worksheet_title": "シフト9月",             # またはタイトル指定
#     "gid": 123456789,                           # または gid 指定
#     "avoid_list": ["鈴木花", "中村結衣"]
#   }
# 備考:
# - グローバルの SHEETS_ID は「共通シート」を使う場合の既定値です。
# - ユーザーごとの指定がある場合は、アプリ側の実装でその値を優先して使う設計に拡張可能です。
# - 現状のAPIエンドポイント（/jobs/notify?user=...）から個別ユーザーの処理を呼び出せます。
