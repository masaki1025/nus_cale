from typing import Any, Dict


def load_mapping() -> Dict[str, Any]:
    """シート構造のマッピング設定を読み込む（スタブ）。

    戻り値:
        マッピング設定の辞書（例: 列名→意味、行種別など）

    TODO:
    - 外部ファイル（YAML/JSON）またはDBからのロード
    - バージョン管理とホットリロード
    """
    return {}
